import { NgxLetterImageAvatarDirective } from './ngx-letter-image-avatar.directive';

describe('NgxLetterImageAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxLetterImageAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
